#include<stdio.h>
#include<conio.h>

int main()
{
    int a, b, sum, difference, product, quotient;
    printf("Enter the first number: ");
    scanf("%d", &a);
    printf("Enter the second number: ");
    scanf("%d", &b);
    
    return 0;
}